import React from 'react';

const Settings: React.FC = () => (
  <div>
    <h2 className="text-2xl font-semibold mb-4">Settings</h2>
    <p>Configure SaaS platform settings. (Connect to backend endpoint)</p>
  </div>
);

export default Settings;
