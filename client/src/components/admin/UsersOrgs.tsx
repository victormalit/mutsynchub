import React from 'react';

const UsersOrgs: React.FC = () => (
  <div>
    <h2 className="text-2xl font-semibold mb-4">User & Organization Management</h2>
    <p>Manage users and organizations. (Connect to backend endpoint)</p>
  </div>
);

export default UsersOrgs;
