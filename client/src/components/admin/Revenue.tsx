import React from 'react';

const Revenue: React.FC = () => (
  <div>
    <h2 className="text-2xl font-semibold mb-4">Revenue</h2>
    <p>View SaaS revenue analytics. (Connect to backend endpoint)</p>
  </div>
);

export default Revenue;
