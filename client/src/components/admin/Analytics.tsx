import React from 'react';

const Analytics: React.FC = () => (
  <div>
    <h2 className="text-2xl font-semibold mb-4">Analytics</h2>
    <p>View analytics dashboards and reports. (Connect to backend endpoint)</p>
  </div>
);

export default Analytics;
